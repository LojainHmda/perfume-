// Audio disabled per user preference
export const useAudio = () => {
  const playClick = () => {};
  const playSealSnap = () => {};
  const playBoxOpen = () => {};
  const playPeelSound = () => {};

  return { playClick, playSealSnap, playBoxOpen, playPeelSound };
};

